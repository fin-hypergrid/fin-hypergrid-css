{
  "name": "fin-hypergrid-browserify-template",
  "version": "1.0.0",
  "description": "Repo template to \"require\" the fin-hypergrid npm module and build a single .js file using gulp-browserify",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathan Eiten <jeiten@openfin.co>",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-browserify": "^0.5.1",
    "gulp-util": "^3.0.8"
  },
  "dependencies": {
    "fin-hypergrid": "^1.3"
  }
}
